/**
 * Which student experience flow we're building.
 * - 'autonomy': Student makes decisions and clicks to navigate. AI is present (e.g. "Tell me what to do", in-context help) but does not drive the flow.
 * - 'ai-guided': AI moves them through everything; minimal clicking. (To build second.)
 */
export const ACTIVE_FLOW = 'autonomy'
